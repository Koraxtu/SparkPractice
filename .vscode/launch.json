{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach: debugpy (PySpark driver in container)",
      "type": "python",
      "request": "attach",
      "connect": { "host": "localhost", "port": 5678 },
      "pathMappings": [
        { "localRoot": "${workspaceFolder}", "remoteRoot": "/opt/app" }
      ]
    }
  ]
}
